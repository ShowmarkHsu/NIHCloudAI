# LLM 只能處理帶來源引用的標準化病人快照

健保雲端的原始資料格式會隨資料類型與頁面端點而異。系統先將來源紀錄正規化成病人快照，再用確定性規則產生臨床事實與安全訊號；LLM 只能整理這些內容，且輸出的每個摘要項目都必須引用來源紀錄。這會增加前置建模成本，但能隔離健保頁面變動、降低模型臆測，並讓醫事人員核對每項陳述。

## Consequences

- DOM、授權 token 與健保 API 原始回應不會直接出現在 prompt 中。
- 規則結果和模型文字可分開測試、版本化及停用。
- 無來源引用或無法通過 schema 驗證的模型輸出不得顯示為臨床事實。
